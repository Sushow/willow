name: build-willow

on:
  workflow_dispatch:
    inputs:
      wake_word:
        description: 'Wake Word'
        required: true
        default: 'hey_jarvis'
        type: choice
        options:
        - hey_jarvis
        - hi_esp
        - alexa
      board:
        description: 'Board'
        required: true
        default: 'ESP32-S3-BOX'
        type: choice
        options:
        - ESP32-S3-BOX

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      # C'est ici la correction : on utilise l'adresse historique (Tovera) qui existe vraiment
      image: ghcr.io/tovera/willow-build:main
      options: --user root

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build Willow
        # J'ai ajouté 'git config' pour éviter une erreur de sécurité courante sur GitHub
        run: |
          git config --global --add safe.directory '*'
          ./utils.sh build ${{ inputs.wake_word }} ${{ inputs.board }} --mode client

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: willow-dist
          path: dist
